import 'package:flutter_test/flutter_test.dart';

/// TODO: When categories gain a Firebase sync handler, add full
/// Hive ↔ SyncService ↔ Firestore round-trip tests here.
void main() {
  test('category sync pipeline TODO', () {
    // This is intentionally a no-op placeholder so CI passes.
    // See lib/docs/feature_sync_status.md for current status.
    expect(true, isTrue);
  });
}
